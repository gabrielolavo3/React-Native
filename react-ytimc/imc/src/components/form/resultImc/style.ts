import { StyleSheet } from "react-native"
import ResultImc from "."

export const styles = StyleSheet.create({
    resultImc: {
        flex: 1,
        marginTop: 15,
        paddingTop: 60,
        borderRadius: 50,
        alignItems: 'center',
        width: '100%'
    },

    infoImc: {
        fontSize: 18,
        color: '#FF0043',
        fontWeight: 'bold'
    },

    valorImc: {
        fontSize: 48,
        color: '#FF0043',
        fontWeight: 'bold'
    }
})